# PathMiner
PathMiner is a minimal Chrome MV3 extension for passive + on-demand endpoint discovery. It captures network requests, extracts paths from DOM/scripts, hooks fetch/XHR when you click Run, scores &amp; tags endpoints, and can parse OpenAPI JSON plus optional GraphQL helpers. Privacy-first: no headers/cookies/bodies stored.
